/** The desktop app's display name. A product name, never a translation key (ruling R9). */
export const SESSRUMNIR_PRODUCT_NAME = 'Sessrúmnir'

/** The fork's name, kept as an alias so an unwalked import still shows ours. */
export const PI_DESKTOP_PRODUCT_NAME = SESSRUMNIR_PRODUCT_NAME
